#include "Organizer.h"
#include <stdio.h>
#include <windows.h>

using namespace std;

int main()
{
    Organizer();

    /*
    HANDLE hConsole = GetStdHandle(STD_OUTPUT_HANDLE);

    cout << "ieghwovrbghvwru";

    SetConsoleTextAttribute(hConsole, (WORD) ((0 << 4) | 14));
	printf("Hello");

	SetConsoleTextAttribute(hConsole, (WORD) ((0 << 4) | 7));
	cout << 1;
	*/
}

